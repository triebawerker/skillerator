{% extends "TriebawerkeSkilleratorBundle::layout.html.twig" %}

{% block title %}Skillerator{% endblock %}

{% block content %}
<h1>Team creation</h1>

<form action="{{ path('team_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('team') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}